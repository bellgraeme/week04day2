<!DOCTYPE html>
<html>
<head>
    <title></title>
    <link rel = './public/stylesheet' href = 'style.css'/>
</head>
<body class = "body">

    <form method = "POST" action="/pizzas/<%=@pizza.id%>"> 

      <label for = "first-name">First Name:</label>
      <input type = "text" 
      name= "first_name"
      id= "first-name"/ 
      value = "<%= @pizza.first_name %>"> <!-- name property must match the hash from the object/db--> 

      <label for ="last-name">Last Name</label>
      <input type = "text"
      id = "last-name" 
      name = "last_name" 
      value = "<%= @pizza.last_name %>"/>

      <label for = "pizza-select"> Select Pizza:</label>
      <select id = "pizza-select" name ="topping">
        <option <%="selected" if @pizza.topping == "Margherita" %> value = "Margherita">Margherita</option>
        <option <%="selected" if @pizza.topping == "Calzone" %> value = "Calzone">Calzone</option>
        <option <%="selected" if @pizza.topping == "Peperoni" %> value = "Peperoni">Peperoni</option>
        <option <%="selected" if @pizza.topping == "Italian Sausage" %> value = "Italian Sausage">Italian Sausage</option>
    </select>


    <label for = "quantity">Quantity:</label>
    <input  min="1" step = "1" type ="number" name="quantity" id ="quantity" value="<%= @pizza.quantity %>" />

    <input type = "submit" value="Update Order"/>
</form>
</body>
</html>